const Registry = () => {
  return (
    <section className="registry">
      <h2>Registry</h2>
      <p>Your presence is the best present, but here are a few things we’d love.</p>
    </section>
  );
};

export default Registry;
